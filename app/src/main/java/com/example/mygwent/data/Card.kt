package com.example.mygwent.data


class Card {
/*
    sealed class Card(
        open val id: Int,
        open val name: String,
        open val description: String,
        open val type: CardType,
        open val effect: Effect?
    ) {
        data class AttackCard(
            override val id: Int,
            override val name: String,
            override val description: String,
            override val type: CardType,
            override val effect: Effect?,
            val power: Int
        ) : Card(id, name, description, type, effect)

        data class SpecialCard(
            override val id: Int,
            override val name: String,
            override val description: String,
            override val type: CardType,
            override val effect: Effect
        ) : Card(id, name, description, type, effect)

        data class WeatherCard(
            override val id: Int,
            override val name: String,
            override val description: String,
            val weatherType: WeatherType
        ) : Card(id, name, description, CardType.WEATHER, null)
    }

    enum class CardType {
        CLOSE_COMBAT, RANGED, SIEGE, SPECIAL, WEATHER
    }

    data class Effect(val name: String, val description: String)

 */
}


